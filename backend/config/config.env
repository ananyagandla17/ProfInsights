# Runtime environment
NODE_ENV=development
PORT=5000

# MongoDB Atlas Connection String (corrected with real cluster + password)
MONGO_URI=mongodb+srv://niharika:nihar317@profinsights.wc9dyfw.mongodb.net/profinsights?retryWrites=true&w=majority&appName=profinsights

# JWT Auth Config
JWT_SECRET=this_is_a_super_secure_long_secret_key
JWT_EXPIRE=30d
JWT_COOKIE_EXPIRE=30

# Email Configuration (Mailtrap for dev/testing)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_EMAIL=your_mailtrap_username
SMTP_PASSWORD=your_mailtrap_password
FROM_EMAIL=noreply@profinsights.mahindrauniversity.edu.in
FROM_NAME=ProfInsights

# Security
IP_HASH_SECRET=random_ip_hashing_key_here

# Frontend URL
CLIENT_URL=http://localhost:3000
